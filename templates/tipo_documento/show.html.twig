{% extends 'base.html.twig' %}

{% block title %}TipoDocumento{% endblock %}

{% block body %}
    <h1>TipoDocumento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_documento.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ tipo_documento.nome }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ tipo_documento.descricao }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_tipo_documento_index') }}">back to list</a>

    <a class="btn btn-secondary" href="{{ path('app_tipo_documento_edit', {'id': tipo_documento.id}) }}">edit</a>

    {{ include('tipo_documento/_delete_form.html.twig') }}
{% endblock %}
